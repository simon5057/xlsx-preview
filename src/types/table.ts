export interface CellSpan {
  col?: number;
  row?: number;
}

export interface RowsAndColsSpanMap {
  rows: number[];
  cols: number[];
}

export interface ImagePosAndSize {
  left: number;
  top: number;
  width: number;
  height: number;
}
